/* Arrays
1. Different types of values can be stored in an array.
2. Size of the array is dynamic i.e you can add elements in it anytime.
*/
let selectedColors = ['red', 'blue'];
selectedColors[2] = 1;
console.log(selectedColors.length);